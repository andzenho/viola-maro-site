name: Публикация на GitHub Pages

# Сборка под собственный домен: без префикса подпути и без запрета
# индексации.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Установить Pillow
        run: pip install --quiet pillow

      - name: Собрать сайт с оплатой
        run: python3 build.py

      - name: Собрать версию предзаписи
        run: python3 build.py --mode pre --out site/pre

      - name: Собрать страницу брони
        run: python3 build.py --mode bron --out site/bron

      - name: Собрать страницу заявки
        run: python3 build.py --mode zayavka --out site/zayavka

      - name: Собрать событие «Неудобные»
        run: python3 build.py --mode neudobnye --out site/neudobnye

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
